---
name: md2docx-gbt9704
description: 将 Markdown 按 GB/T 9704-2012 党政机关公文格式标准转换为 DOCX
---

# GB/T 9704-2012 Markdown to DOCX 转换器

## 快速开始

将符合规范的 Markdown 文件转换为符合 GB/T 9704-2012 标准的 Word 文档：

```bash
python3 scripts/md2docx.py input.md [output.docx]
```

输出文件命名规则：
- 指定输出：使用指定文件名
- 未指定：`{输入文件名}_format.docx`（例如：`report.md` → `report_format.docx`）

## 前置条件

使用前请确认：
- 已安装 Python 3.x
- 已安装 `python-docx`：`pip install python-docx`
- 系统已安装必需字体（Windows 用户通常已具备，Mac/Linux 用户需额外安装）

## 触发场景

此 skill 适用于以下场景：
- 需要生成党政机关公文格式文档
- 要求使用 GB/T 9704-2012 标准
- 需要按特定格式（边距、字体、行距、页码）输出 Word 文档
- 将 Markdown 转换为符合国标的 DOCX

## 标准规格

| 项目 | 规格 |
|------|------|
| 纸张 | A4 |
| 上边距 | 37mm |
| 下边距 | 35mm |
| 左边距 | 27mm |
| 右边距 | 27mm |
| 行间距 | 固定值 28 磅 |
| 每页行数 | 可视情况增删 1-2 行 |

## 字体规范

| 元素 | 字体 | 字号 | 加粗 | 对齐 |
|------|------|------|------|------|
| 主标题 | 方正小标宋简体 | 2号（22pt） | 否 | 居中 |
| 一级标题（一、） | 黑体 | 3号（16pt） | 是 | 左对齐 |
| 二级标题（（一）） | 楷体_GB2312 / 楷体 | 3号（16pt） | 是 | 左对齐 |
| 三级标题（1.） | 仿宋_GB2312 / 仿宋 | 3号（16pt） | 是 | 左对齐 |
| 四级标题（（1）） | 仿宋_GB2312 / 仿宋 | 3号（16pt） | 否 | 左对齐 |
| 正文 | 仿宋_GB2312 / 仿宋 | 3号（16pt） | 否 | 两端对齐 |
| 表头 | 仿宋_GB2312 / 仿宋 | 小四号（12pt） | 是 | 居中 |
| 表格内容 | 仿宋_GB2312 / 仿宋 | 小四号（12pt） | 否 | 左对齐 |
| 落款（单位/日期） | 仿宋_GB2312 / 仿宋 | 3号（16pt） | 否 | 右对齐 |
| 页码 | Times New Roman | 4号（14pt） | 否 | 奇数页居右，偶数页居左 |

## 标题层级结构

正文层次依次使用：
1. **一、** → 一级标题，黑体加粗
2. **（一）** → 二级标题，楷体加粗
3. **1.** → 三级标题，仿宋加粗
4. **（1）** → 四级标题，仿宋不加粗

## 排版规则

- 所有段落（包括标题和正文）首行缩进 2 字符
- 标题与正文之间在源文件中空一行（生成的 DOCX 会自动处理间距）
- 页码：格式为 `— N —`，奇数页居右，偶数页居左
- 落款：单位名称和日期右对齐，正文下空两行

## Markdown 映射规则

| Markdown 语法 | 识别为 | 格式 |
|--------------|--------|------|
| `# 标题` | 主标题 | 方正小标宋，22pt，居中 |
| `一、xxx` 或包含该模式的段落 | 一级标题 | 黑体加粗，16pt |
| `（一）xxx` 或包含该模式的段落 | 二级标题 | 楷体加粗，16pt |
| `1. xxx` | 三级标题 | 仿宋加粗，16pt |
| `（1）xxx` 或包含该模式的段落 | 四级标题 | 仿宋不加粗，16pt |
| `* 文本` 或 `- 文本` | 列表项 | 去除标记，作为正文 |
| `**文本**` | 加粗标记 | 标题中保留加粗，正文中去除 |
| `\| 表格 \|` | 表格 | 仿宋12pt，表头加粗 |

### 落款格式

- 单位名称（以公司/局/委/办等结尾）：右对齐，正文下空两行
- 日期（2026年1月3日格式）：右对齐，单位名称下空一行

## 推荐 Markdown 写法

```markdown
# 关于推进数字化转型工作的实施方案

一、总则

本文档用于说明相关事项。

（一）总体目标

本年度工作以数字化转型为核心，全面提升信息化水平。

（二）基本原则

1. 坚持党建引领
2. 坚持创新驱动

二、重点任务

1. 推进数字化建设

加快推进信息系统的整合与升级，确保数据安全。

（1）信息系统升级

加快现有信息系统的升级改造，确保数据安全。

（2）数据治理

建立完善的数据治理体系。

| 项目 | 要求 |
|------|------|
| 数据采集 | 规范化 |
| 数据存储 | 安全化 |

某某单位

2026年1月3日
```

**说明**：
- 一级标题 `一、` 和二级标题 `（一）` 直接作为段落文本，不需要 Markdown 标题符号 `#`
- 三级标题使用 Markdown 有序列表 `1.`
- 四级标题 `（1）` 同样作为段落文本
- 主标题使用 `# 标题` 格式

## 字体跨平台说明

脚本会自动适配系统可用字体：

| 字体类型 | Windows | Mac | Linux |
|---------|---------|-----|-------|
| 主标题 | 方正小标宋简体 | 方正小标宋简体 | 尝试检测，无则回退 |
| 仿宋 | 仿宋_GB2312 | 仿宋 | 尝试检测 |
| 黑体 | 黑体 | STHeiti / 黑体 | 尝试检测 |
| 楷体 | 楷体_GB2312 | 楷体 | 尝试检测 |

**注意**：
- Mac 用户需单独安装方正小标宋简体字体
- Linux 系统若无中文字体，Word 打开时会回退到默认字体
- 建议在 Windows 系统或安装完整中文字体的系统上生成文档

## 当前限制

以下功能暂不支持，使用时请注意：
- **图片**：会被忽略，不会插入到 DOCX 中
- **嵌套表格**：仅支持简单表格
- **代码块**：会被转换为普通文本段落
- **行内代码**：会被转换为普通文本
- **链接**：保留文本，去除链接格式

## 常见问题

**Q: 字体显示为默认字体？**  
A: 系统缺少对应字体。Windows 用户可从字体库安装；Mac 用户需单独安装方正小标宋简体；Linux 用户需安装完整的中文字体包。

**Q: 如何调整行距？**  
A: 打开 `scripts/md2docx.py`，修改 `LINE_SPACING` 变量（推荐值为 26-30 磅）。

**Q: 页码未显示或位置错误？**  
A: 在 Word/WPS 中打开文档后，右键点击页码区域，选择"更新域"。某些版本的 Word 需要在页眉页脚编辑模式下手动设置奇偶页不同。

**Q: 一级标题、二级标题没有正确识别？**  
A: 确保标题文本格式正确：`一、`、`（一）`、`（1）` 等需要使用中文全角标点。可参考"推荐 Markdown 写法"章节的示例。

**Q: 表格格式不正确？**  
A: 确保表格使用标准 Markdown 语法，且每列之间使用 `|` 分隔。复杂表格建议在生成后手动调整。

**Q: 生成的文档段落间距不对？**  
A: 这是正常现象。GB/T 9704-2012 标准使用固定行距（28 磅），段落间距由行距统一控制。如需调整，可在生成后手动微调。

## 技术说明

- 依赖库：`python-docx`
- 兼容性：Python 3.6+
- 输出格式：`.docx`（Office Open XML）
- 字体回退：当指定字体不存在时，自动使用系统默认字体
